<?php

/**
 * @file
 * Callbacks and common functions for the WYSIWYG Ace plugin.
 */

/**
 * Implements hook_wysiwyg_include_directory().
 */
function wysiwyg_ace_wysiwyg_include_directory($type) {
  if ($type == 'editors') {
    return 'editors';
  }
}
